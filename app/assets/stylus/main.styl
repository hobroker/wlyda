@import "_mixins.styl"

.d-center {
	text-align center
}
.btn-fb {
	color #fff
	background-color $fb-btn-color
	border-color $fb-btn-color
	cursor: pointer
	&:hover {
		background-color darken($fb-btn-color, 10%)
		border-color darken($fb-btn-color, 10%)
	}
}

